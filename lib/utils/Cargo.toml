[package]
edition = "2021"
name = "utils"
publish = false
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libc = "0.2"
rustix = {version = "0.35.9", features = ["process", "fs"]}
